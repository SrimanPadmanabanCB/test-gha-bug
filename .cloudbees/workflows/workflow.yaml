apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: Example

on:
  push:
     branches:
       - '**'

jobs:
  analyze-pr:
    steps:
      - name: Check Quality Score
        uses: docker://alpine:3.20
        shell: sh
        run: |
          echo "${{ cloudbees.event }}"
